# ============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file and rename it to: .env
#    Command: cp .env.example .env
#
# 2. Replace the API keys below with your actual keys
#
# 3. Get your keys from:
#    - Anthropic: https://console.anthropic.com/settings/keys
#    - OpenAI: https://platform.openai.com/api-keys
#
# 4. IMPORTANT: Never commit the .env file to version control!
#    (It's already in .gitignore)
# ============================================================================

# Anthropic API Key (required for documentation generation)
ANTHROPIC_API_KEY=your-api-key-here

# OpenAI API Key (optional - only needed if embeddings are enabled)
# Enable embeddings in .codedocsrc.json: { "embeddings": { "enabled": true } }
OPENAI_API_KEY=your-openai-api-key-here
